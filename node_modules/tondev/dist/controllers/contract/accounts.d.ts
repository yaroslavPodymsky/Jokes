import { Terminal } from "../../core";
import { Account } from "@tonclient/appkit";
export declare function getAccount(terminal: Terminal, args: {
    file: string;
    network: string;
    signer: string;
    data: string;
    address?: string;
}): Promise<Account>;
//# sourceMappingURL=accounts.d.ts.map